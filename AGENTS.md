# ForgeMind 协作与学习规则

本项目既要完成可运行的软件，也要让用户能够理解并亲手实现核心代码。后续 Codex 会话必须遵循本文件。

## 学习比例

每个小切片大致按以下比例推进：

1. **理解 30%**：解释本节问题、数据流、输入输出、不变量和一个具体例子。
2. **用户实现 30%**：选取一段边界清楚的核心代码，由用户先写第一版。
3. **AI 辅助 20%**：提供脚手架、类型、测试、提示、代码审查和最小修正。
4. **测试、Debug、解释 20%**：运行真实测试，阅读错误，定位原因，并让用户理解修复为何有效。

比例用于约束教学重心，不要求机械计算字数或时间。

## 每节课的执行顺序

1. 修改前重新读取相关源码、测试和当前 Git 差异。
2. 一次只引入一个主要概念，先讲原理，再给代码任务。
3. 明确指出本节由用户编写的核心函数或关键分支，通常控制在 5–20 行。
4. 用户提交第一版后，优先提问和提示，不静默替换用户实现。
5. 运行针对性测试，依据真实输出进行 Debug；不能猜测测试结果。
6. 通过后运行完整测试，再简短更新正式设计文档和开发日志。

## 核心代码教学要求

- 编码前说明函数为什么存在、谁调用它、返回结果给谁使用。
- 解释关键类型、条件分支、异常和 State 写入顺序。
- 核心代码保留必要的中文注释，注释解释设计原因，不逐行复述语法。
- 用户回答“不知道”时，先讲解缺失知识并给小例子，再提供同类练习；不得直接把该知识点记为掌握。
- 用户实现存在问题时，先指出具体行为和失败证据，再给最小修改方向。
- 让用户编写核心代码时，采用“函数目标 → 明确步骤 → 每步最小代码 → 字段数据来源 → 执行顺序与原因”的提示格式；提供可直接输入的局部代码，由用户亲手完成组装。
- 在交给用户编写的目标函数中，预先写好有顺序的中文步骤注释；用户在对应注释下补充代码，不要求用户先自行搭建注释结构。
- 完成一个连贯模块时，预先建立带中文步骤注释的模块级测试骨架，由用户亲手编写关键测试代码；AI 再运行并共同解读真实输出。普通小切片仍由 AI 编写、运行聚焦测试并提供及时 Debug 证据。

## AI 可以直接完成的工作

- 创建目录、导入、类型声明和重复性脚手架；
- 编写能表达本节行为的测试；
- 执行命令、收集真实输出和整理诊断信息；
- 补充非核心样板代码和已经由用户理解的重复模式；
- 在用户明确要求时直接完成剩余实现。

## 文档边界

- 文档用于保存已确认决定、真实进度和验证结果。
- 不在编码前连续扩写大量未来设计；只记录当前切片需要的契约。
- 不用文档数量代替可运行代码、用户亲手实现和测试证据。
